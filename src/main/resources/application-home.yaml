spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/profiles
    username: postgres
    password: PkNLm@#2357
  jpa:
    properties:
      hibernate:
        "[format_sql]": true
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    web: debug
    sql: error
    "[org.springframework.security]": trace
    "[com.hangout.core]": trace

otel:
  exporter:
    otlp:
      endpoint: http://localhost:4317
  instrumentation:
    micrometer:
      enabled: false

aws:
  region: ap-south-1

hangout:
  allowed-origins:
    url: http://localhost:5137/,
  media:
    upload-bucket: profile-photos
  auth-service:
    url: http://localhost:80
